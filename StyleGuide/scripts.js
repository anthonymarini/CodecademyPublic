const LOG = true;

function log( msg ) {
    if ( LOG ) {
        console.log( msg );
    }
}

function changeColorValue( num ) {
    log( "Number " + num + " was chosen." );
    switch( num ) {
        case 0: 
            break;
        case 1:
            break;
        case 2:
            break;
        default: 
            break;
    }
}